# setup

```
mkdir chapter05
cd chapter05
pnpm init


pnpm add react react-dom
pnpm add -D typescript vite @types/react @types/react-dom biome @vitejs/plugin-react

pnpm tsc --init

mkdir src
touch index.html src/main.tsx
```

